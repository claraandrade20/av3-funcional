# Referência opcional — o backend lê a chave diretamente de services.clj.
# Este arquivo serve apenas como lembrete; não é carregado automaticamente pelo Leiningen.

# API Ninjas Key — https://api-ninjas.com (free tier: 60 requisições/mês)
API_NINJAS_KEY=sua-chave-aqui

# Porta padrão do servidor Ring
PORT=3000
